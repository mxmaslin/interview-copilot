---
description: Copilot — ответы для sidecar / транскрипта
alwaysApply: true
---

# Copilot

Сессия интервью — только через **menubar sidecar (CP)**: «Начать интервью», транскрипт, «Ответ на последний вопрос» (`⌘↩`). См. `AGENTS.md`.

Ответы на теорию: RU + EN-термины, структура «определение → пример → оговорки», без воды.

Транскрипт: `data/transcript.md` (`[Интервьюер]:` / `[Я]:`). Sidecar по `⌘↩` печатает ответ **в терминал по чанкам** (`TERMINAL_ANSWER_STREAM=1`); копия — `data/last-answer.md`. macOS-нотификации отключены.
